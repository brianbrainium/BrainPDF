Use pnpm for all package management commands.
Run tests with `pnpm test`.
Avoid modifying or adding binary files. Changes to binary assets like images or icons
will prevent the PR from being created.
